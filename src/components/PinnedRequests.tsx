import { Box, Text } from "@chakra-ui/react";
import { pinnedRequestHeight } from "../configs";

const PinnedRequests = () => {
  return (
    <Box textAlign="center" height={pinnedRequestHeight}>
      <Text>Pinned Requests</Text>
    </Box>
  );
};

export default PinnedRequests;

/**
 * Alias
 * Description
 * Creation Date
 * Arrow Icon to Redirect
 */
