import { Box } from "@chakra-ui/react";
import React from "react";
import { statusSummaryHeight } from "../configs";

const StatusSummary = () => {
  return <Box height={statusSummaryHeight}>Status Summary </Box>
};

export default StatusSummary;
